<% content_for :title, "Edit #{@customer.name} — Covenant" %>

<%= render "shared/canvas_head", crumbs: [["Customers", customers_path], [@customer.name, customer_path(@customer)], ["Edit", nil]] %>

<header class="perma-header">
  <h1 class="perma-header__title">Edit customer</h1>
</header>

<div class="u-center u-center-narrow">
  <%= render "form", customer: @customer %>
</div>
