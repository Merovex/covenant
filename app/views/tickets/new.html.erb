<% content_for :title, "New ticket — Covenant" %>

<%= render "shared/canvas_head", crumbs: [["Tickets", tickets_path], ["New ticket", nil]] %>

<header class="perma-header">
  <h1 class="perma-header__title">New ticket</h1>
</header>

<%= render "form", ticket: @ticket, url: tickets_path %>
